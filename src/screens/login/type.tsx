export interface LoginFormValues {
    username: string;
    password: string;
  }

  export interface TokenValue {
    token: string;
  }

  export interface LoginUserData {
    Role: string;
    User_id: number;
    exp: number;
    username: string;
  }
  
